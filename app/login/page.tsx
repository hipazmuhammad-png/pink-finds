 "use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {supabaseBrowser} from "@/lib/supabase-browser";
export default function Login(){const[e,setE]=useState("");const[p,setP]=useState("");const[err,setErr]=useState("");const r=useRouter();async function go(x:React.FormEvent){x.preventDefault();const{error}=await supabaseBrowser().auth.signInWithPassword({email:e,password:p});if(error)setErr(error.message);else r.push("/admin")}return <main className="adminWrap"><div className="login panel"><div className="brand">pink<span>finds</span> ✦</div><h2>Admin Login</h2><form onSubmit={go} className="formgrid"><div className="field full"><label>Email</label><input type="email" required value={e} onChange={x=>setE(x.target.value)}/></div><div className="field full"><label>Password</label><input type="password" required value={p} onChange={x=>setP(x.target.value)}/></div>{err&&<div className="notice full">{err}</div>}<button className="btn primary full">Masuk</button></form></div></main>}