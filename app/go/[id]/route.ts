import {NextRequest,NextResponse} from "next/server";import {supabaseServer} from "@/lib/supabase-server";export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await supabaseServer();const{data:p}=await s.from("products").select("affiliate_url,published").eq("id",id).single();if(!p?.affiliate_url||!p.published)return NextResponse.redirect(new URL("/",req.url));await s.from("click_events").insert({product_id:id});return NextResponse.redirect(p.affiliate_url)}